export const metadata = {
  title: " Frequently asked questions  | Ayesha's Agency",
  description: "Find answers to the most common questions about our services.",
};

export default function RootLayout({ children }) {
  return children;
}